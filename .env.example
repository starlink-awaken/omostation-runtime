# Runtime — Environment Variables
# Copy to .env and fill in values (CHANGE_ME) before running

# Paths
RUNTIME_HOME=~/runtime
RUNTIME_MATRIX=~/runtime/matrix.yaml
OMO_DIR=~/.omo

# Secrets (CHANGE_ME before deploy)
AGORA_JWT_SECRET=CHANGE_ME_RANDOM_SECRET
